<?xml version="1.0" encoding="utf-8"?>
<!-- Convention file for ILRepack.Lib.MSBuild.Task: merges NVorbis into UnknownsCollection.dll
     after every build, so the mod stays a single deliverable file (Paket O, Ogg migration).
     Internalize keeps the NVorbis types out of the public surface. The LibraryPath entries let
     ILRepack re-resolve every compile-time reference (game interop, BepInEx, TOR) plus 0Harmony,
     which BepInEx provides only at runtime. -->
<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
    <Target Name="ILRepacker" AfterTargets="Build">
        <ItemGroup>
            <InputAssemblies Include="$(TargetPath)" />
            <InputAssemblies Include="$(TargetDir)NVorbis.dll" />
            <LibraryPath Include="$(TargetDir)" />
            <LibraryPath Include="@(ReferencePathWithRefAssemblies->'%(RootDir)%(Directory)')" />
            <LibraryPath Include="$(NuGetPackageRoot)harmonyx\2.10.2\lib\netstandard2.0" />
        </ItemGroup>
        <ILRepack Parallel="true"
                  Internalize="true"
                  InputAssemblies="@(InputAssemblies)"
                  LibraryPath="@(LibraryPath)"
                  OutputFile="$(TargetPath)" />
    </Target>
</Project>
